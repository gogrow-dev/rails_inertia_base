# Supply-chain cooldown: refuse package versions published less than 7 days ago,
# leaving time for malicious releases to be spotted and yanked. Value is in DAYS
# (pnpm and yarn count minutes). Needs npm >= 11.10.
min-release-age=7

# Fail, rather than warn, when Node/npm don't satisfy package.json "engines".
engine-strict=true
